---
AIGC:
    ContentProducer: Minimax Agent AI
    ContentPropagator: Minimax Agent AI
    Label: AIGC
    ProduceID: "00000000000000000000000000000000"
    PropagateID: "00000000000000000000000000000000"
    ReservedCode1: 3046022100a9f9b4a5482c3ecbef327a77f1530f40daa35329f4f78247b71895a9a5747aee022100e26963c4424118349b4df4ff5a842f2d034847311c6d1a7a87ea8310be011edf
    ReservedCode2: 304502204f2f198d1fcccf3f1bb54cfaa0fd1f44eb3a77ce0c20b54c6a15a11cf9ff31020221008072b5a3e381187952a67f93edd5e03e6f0522e96635cf61fc96e61745a2a756
---

# 🛠️ Flutter & Android 环境完整安装指南

## 🎯 概述

本指南将帮助您在各种操作系统上安装完整的Flutter和Android开发环境，以便构建"即刻"AI Agent助理应用的APK。

## 📋 系统要求

### 最低要求
- **内存**: 8GB RAM (推荐 16GB+)
- **存储**: 50GB 可用空间
- **网络**: 稳定的互联网连接

### 支持的操作系统
- **Linux**: Ubuntu 18.04+, Debian 10+, CentOS 8+
- **macOS**: 10.14+ (Mojave或更高版本)
- **Windows**: Windows 10+, Windows 11

## 🔧 Linux环境安装

### Ubuntu/Debian

#### 1. 更新系统
```bash
sudo apt update && sudo apt upgrade -y
```

#### 2. 安装基础工具
```bash
sudo apt install -y curl wget git unzip xz-utils zip tar
sudo apt install -y lib32z1 lib32stdc++6
```

#### 3. 安装Flutter
```bash
# 下载Flutter
cd ~/
wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.16.0-stable.tar.xz

# 解压Flutter
tar xf flutter_linux_3.16.0-stable.tar.xz

# 添加到PATH
echo 'export PATH="$PATH:$HOME/flutter/bin"' >> ~/.bashrc
source ~/.bashrc

# 验证安装
flutter doctor
```

#### 4. 安装Android SDK
```bash
# 创建Android SDK目录
mkdir -p ~/android-sdk/cmdline-tools
cd ~/android-sdk

# 下载Command Line Tools
wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
unzip commandlinetools-linux-9477386_latest.zip

# 设置环境变量
echo 'export ANDROID_HOME=$HOME/android-sdk' >> ~/.bashrc
echo 'export PATH=$PATH:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin' >> ~/.bashrc
source ~/.bashrc

# 安装必要组件
sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
sdkmanager "system-images;android-34;google_apis;x86_64"  # 模拟器镜像
sdkmanager "emulator"

# 接受许可证
yes | sdkmanager --licenses
```

#### 5. 安装Java JDK
```bash
sudo apt install -y openjdk-17-jdk
java -version
```

#### 6. 安装额外工具
```bash
# 图片优化工具
sudo apt install -y pngquant jpegoptim webp-tools imagemagick

# 其他开发工具
sudo apt install -y vim neovim htop tree
```

#### 7. 完成Flutter设置
```bash
# 接受Android许可证
flutter doctor --android-licenses

# 创建Android虚拟设备 (可选)
sdkmanager "system-images;android-34;google_apis;x86_64"
avdmanager create avd -n "flutter_emulator" -k "system-images;android-34;google_apis;x86_64"
```

### CentOS/RHEL

#### 1. 更新系统
```bash
sudo yum update -y
```

#### 2. 安装开发工具
```bash
sudo yum groupinstall -y "Development Tools"
sudo yum install -y wget curl git unzip zip tar
sudo yum install -y java-11-openjdk-devel
```

#### 3. 安装Flutter (同Ubuntu步骤3)

#### 4. 安装Android SDK (同Ubuntu步骤4)

## 🍎 macOS环境安装

### 使用Homebrew (推荐)

#### 1. 安装Homebrew
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

#### 2. 安装Flutter
```bash
brew install --cask flutter

# 或手动安装
wget https://storage.googleapis.com/flutter_infra_release/releases/stable/macos/flutter_macos_3.16.0-stable.zip
unzip flutter_macos_3.16.0-stable.zip
export PATH="$PATH:`pwd`/flutter/bin"

# 永久添加到PATH
echo 'export PATH="$PATH:$HOME/flutter/bin"' >> ~/.zshrc
source ~/.zshrc
```

#### 3. 安装Android Studio
```bash
brew install --cask android-studio
```

#### 4. 安装模拟器
```bash
# 从Android Studio安装模拟器
# 或者使用命令行
sdkmanager "system-images;android-34;google_apis;x86_64"
sdkmanager "emulator"
avdmanager create avd -n "flutter_emulator" -k "system-images;android-34;google_apis;x86_64"
```

#### 5. 安装额外工具
```bash
brew install pngquant jpegoptim webp imagemagick
```

## 🪟 Windows环境安装

### 1. 安装Chocolatey (Windows包管理器)
```powershell
# 以管理员身份运行PowerShell
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```

#### 2. 安装基础工具
```powershell
choco install -y git
choco install -y visualstudio2019buildtools
choco install -y javaruntime
```

#### 3. 安装Flutter
```powershell
# 下载Flutter SDK
$url = "https://storage.googleapis.com/flutter_infra_release/releases/stable/windows/flutter_windows_3.16.0-stable.zip"
$output = "flutter_windows_3.16.0-stable.zip"
Invoke-WebRequest -Uri $url -OutFile $output

# 解压到C:\flutter
Expand-Archive -Path $output -DestinationPath "C:\"
Add-EnvironmentVariable -Path "PATH" -Value "C:\flutter\bin"

# 重启命令行
flutter doctor
```

#### 4. 安装Android Studio
1. 从 https://developer.android.com/studio 下载安装
2. 启动Android Studio
3. 完成初始设置
4. 安装Android SDK和工具

#### 5. 设置环境变量
```powershell
# 添加Android SDK路径
[System.Environment]::SetEnvironmentVariable("ANDROID_HOME", "C:\Users\$env:USERNAME\AppData\Local\Android\Sdk", "User")
[System.Environment]::SetEnvironmentVariable("PATH", "$env:PATH;C:\Users\$env:USERNAME\AppData\Local\Android\Sdk\platform-tools", "User")
```

## 🔍 环境验证

### 检查Flutter安装
```bash
flutter doctor
```

应该看到类似输出：
```
Doctor summary (to see all details, run flutter doctor -v):
[✓] Flutter (Channel stable, 3.16.0, on Linux, locale en_US.UTF-8)
[✓] Android toolchain - develop for Android devices
[✓] Chrome - develop for the web
[✓] Linux toolchain - develop for Linux desktop applications
[✓] Android Studio (version 2022.3)
[✓] Connected device (1 available)
[✓] Network resources

• No issues found!
```

### 检查Android环境
```bash
# 检查Android SDK
adb version

# 检查Android平台工具
sdkmanager --list

# 检查已安装的平台
sdkmanager --list_installed
```

### 测试Flutter项目
```bash
# 创建测试项目
flutter create test_app
cd test_app
flutter run
```

## 🐛 常见问题解决

### 1. Flutter Doctor显示错误

#### Android license问题
```bash
flutter doctor --android-licenses
# 输入 'y' 接受所有许可证
```

#### Android Studio未检测到
```bash
# 设置ANDROID_STUDIO_DIR环境变量
export ANDROID_STUDIO_DIR=/path/to/android-studio
```

### 2. Android SDK权限问题 (Linux)
```bash
# 设置正确的权限
sudo chown -R $USER:$USER $HOME/android-sdk
```

### 3. 内存不足
```bash
# 增加Gradle内存
echo "org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m" >> android/gradle.properties
```

### 4. 网络问题
```bash
# 使用国内镜像源
flutter config --set global pub-hosted-url https://pub.flutter-io.cn
flutter config --set global flutter-mirrors-china.com true
```

## 📱 额外工具安装

### 图片优化工具
```bash
# Ubuntu/Debian
sudo apt install -y pngquant jpegoptim webp-tools imagemagick

# macOS
brew install pngquant jpegoptim webp imagemagick

# Windows (使用Chocolatey)
choco install -y imagemagick
```

### 代码编辑器
```bash
# 安装VS Code和Flutter插件
# Ubuntu
sudo snap install --classic code

# macOS
brew install --cask visual-studio-code
```

### 模拟器工具
```bash
# 创建Android虚拟设备
sdkmanager "system-images;android-34;google_apis;x86_64"
avdmanager create avd -n "flutter_emulator" -k "system-images;android-34;google_apis;x86_64"

# 启动模拟器
emulator -avd flutter_emulator
```

## ✅ 完成检查清单

- [ ] Flutter SDK安装完成
- [ ] Android SDK安装完成
- [ ] Android平台工具安装完成
- [ ] Flutter doctor无错误
- [ ] ADB命令可用
- [ ] 可以运行Flutter doctor -v
- [ ] 可以创建和运行Flutter项目

## 🚀 下一步

环境安装完成后，您就可以开始构建"即刻"AI Agent助理应用了！

```bash
# 1. 进入项目目录
cd jike_ai_agent

# 2. 清理项目
flutter clean

# 3. 获取依赖
flutter pub get

# 4. 运行测试
flutter test

# 5. 构建APK
flutter build apk --release
```

祝您环境搭建顺利！🎉